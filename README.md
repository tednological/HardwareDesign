# HardwareDesign

This project seeks to alter an existing datapath to allow for two more MIPS commands, sllv and ori. I have added a new module, `zeroextender`, along with another mux to create the ori command. For the sllv command, I had to work around some of the logic to permit the R-Type command.
